pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
    }
}
rootProject.name = "CleanArchitecture"
include(":presentation")
include(":data")
include(":domain")
